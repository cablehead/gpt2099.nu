[[name, description, inputSchema]; [list_allowed_directories, "Returns the list of directories that this server is allowed to access. Use this to understand which directories are available before trying to access files.", {type: object, properties: {}, required: []}]]