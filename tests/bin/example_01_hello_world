#!/bin/bash

case "$(uname)" in
    Linux*)
        exec "$(dirname "$0")/example_01_hello_world_linux" "$@"
        ;;
    Darwin*)
        exec "$(dirname "$0")/example_01_hello_world_macos" "$@"
        ;;
    *)
        echo "Unsupported platform: $(uname)" >&2
        exit 1
        ;;
esac